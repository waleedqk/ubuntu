# tmux resurrect

**Start new new session to attach to existing one**

    tmux attach -t base || tmux new -s base

**Save Session**

    prefix + Ctrl-s

**Kill Server**

    tmux kill-server

**Restore Session**

    prefix + Ctrl-r